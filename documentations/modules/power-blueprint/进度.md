# 形态 Power 蓝图模块进度

## 当前阶段

阶段 5 的基础保存导出实现阶段。

## 已完成

- 确认 `powers` 目录存在大量 JSON，约 449 个文件。
- 确认数据结构混合 Origins/Apoli 与 SSC 自定义类型。
- 确认 Java 侧使用 `SerializableData` 注册 power/action/condition。
- 确定 JSON AST 为语义数据源，React Flow graph 为交互投影。
- 已阅读 `蓝图系统设计概览.md`。
- 已确认示例 Power `form_axolotl_fountain_belt_trinket.json` 与示例蓝图的映射关系。
- 已确认本地 Apoli 源码位于 `F:\MC Modding\RefMods\apoli`。
- 已新增 `实现计划.md`，规划从示例 Power 只读生成开始逐步实现。
- 已安装并接入 `@xyflow/react`。
- 已建立 `src/renderer/modules/power-blueprint/` 初始代码目录。
- 已手写覆盖示例 Power 所需的初版 schema registry。
- 已实现 `Power JSON -> PowerAst` 初始解析。
- 已实现 `PowerAst -> Power JSON` 初始序列化，用于后续 roundtrip 验证。
- 已实现 `PowerAst -> BlueprintGraph` 初始只读投影与自动布局。
- 已在 Power 蓝图页面接入 React Flow 只读画布，选中 Power 后读取真实 JSON 并生成节点/连线。
- 已支持从“独立 Power”分类打开未绑定到形态的 Power。
- 已修正 Particle Effect datatype 显示：节点标题固定为 `Particle Effect`，粒子标识符显示为 `type` 字段。
- 已按 power/action/condition/datatype 区分节点背景、边框和输出参数块颜色。
- 已将自动布局间隔调整为 `510 x 234`，即上一版三倍间隔的一半。
- 已将蓝图连线改为直线，移除连线文字标签，并按 power/action/condition/datatype 主题色显示。
- 已将普通标量参数块改为深色底加白色边框，和特殊 slot/datatype 参数块区分。
- 已支持在当前画布中拖动节点调整位置。
- 已支持从输出端口拖动创建连线，并对同一输入节点只接受一条输入做基础限制。
- 已支持从输出端口拖到空白处打开可搜索的新建节点菜单，并按 slot/datatype 类型过滤。
- 已支持按空格或双击画布空白处打开可搜索的新建节点菜单。
- 已支持双击节点打开可选字段弹窗，在当前画布中添加 schema 字段或删除 schema 中的非必填字段。
- 已支持选中节点和连线后用 `Delete` / `Backspace` 删除；删除节点时会同步删除相连连线。
- 已支持按住 `Shift` 多选节点或连线。
- 已支持从空白处拖动框选节点，框选模式为局部重叠即选中。
- 已关闭 React Flow 默认双击缩放，避免与空白处双击新建节点菜单冲突。
- 已按本地 Apoli `AndAction` / `AndCondition` factory 拆分 `origins:and` 的 action/condition 上下文 schema：action 只暴露 `actions`，condition 只暴露 `conditions` 与 `inverted`。
- 已将 `origins:multiple` 作为隐式容器处理：AST 中保留，蓝图 Graph 中不显示容器节点，直接显示多个子 power 根节点。
- 已隐藏 power 节点左侧输入 handle，power 节点只作为蓝图根节点或顶层 power 节点存在。
- 已从新建节点菜单中排除 `origins:multiple`。
- 已支持在节点内直接编辑标量参数，回车或失焦提交。
- 已为 int / float / string / boolean 参数添加基础输入校验；非法输入不会更新画布字段值。
- 已支持过长参数输入框和参数块整体向右延伸，不自动换行。
- 已将 boolean 参数改为节点内开关控件，点击后直接提交 `true` / `false`。
- 已将 slot/datatype 参数块的连接显示状态改为由当前 React Flow edges 推导，删除、重连、拖空白创建新节点后都会同步刷新 `connected/displayValue`。
- 已实现 `BlueprintGraph -> PowerAst -> Power JSON` 的基础导出链路。
- 已接入 Electron IPC 保存能力，`保存当前蓝图` 会写回目标模组 `powers/<power>.json`。
- 已建立 `.ssc-studio/blueprints/<power>.json` 蓝图 UI 状态文件，用于保存节点位置、视口和 graph 快照。
- 已支持打开 Power 时读取 `.ssc-studio/blueprints` 并恢复节点位置与视口。
- 已在保存前校验必填 slot/datatype 是否已连接、数字字段是否合法、是否存在未连接到 power 根节点的孤立节点。
- 已支持同一蓝图存在多个 power 根节点时导出为隐式 `origins:multiple`；单 power 根节点导出时不包裹 `origins:multiple`。
- 已支持在 Power 节点头部直接编辑节点名称；单 power 导出时该名称只作为 Studio 内部标识，多 power 导出时作为 `origins:multiple` 的字段名。
- 已将保存当前蓝图注册到 `Ctrl+S` / `Cmd+S` 快捷键；焦点在输入框内时会先提交输入框草稿再保存。
- 已将节点创建菜单加宽，并按 Origins 文档风格拆分为 Data Types / Power Types / Action Types / Condition Types / Misc 顶层抽屉；Action/Condition 下按文档已有类型分类，不再按推测用途分类。
- 已实现蓝图撤销/重做历史栈，覆盖参数修改、可选参数添加/删除、Power 节点命名、节点创建/删除、节点移动，并提供工具栏按钮与 `Ctrl+Z` / `Ctrl+Y` / `Ctrl+Shift+Z` 快捷键。
- 已将初版手写示例 schema registry 拆分为 `schemaBuilders.ts`、`apoliBuiltinSchemas.ts`、`sscBuiltinSchemas.ts` 与聚合入口 `exampleSchemas.ts`。
- 已基于本地 Apoli 源码补充第一版内置节点库，覆盖常见 power、entity/bientity/block/item action、entity/bientity/block/damage/fluid/item condition、`vector` 与 `particle_effect` datatype。
- 已基于 SSC 项目 `additional_power` 源码补充第一版自定义节点库，覆盖当前项目高频 SSC power/action/condition，包括 Mana、Charge、Scale、饰品、粒子圈、召唤随从、Web、变形等节点。
- 已按 `type + kind` 注册 Apoli meta action / meta condition，避免同名节点在 action 与 condition 上下文中互相污染字段。
- 已增强未知字段推断逻辑，能够按字段名区分 `bientity_action`、`item_action`、`block_action` 以及各类 condition slot。
- 已按 Origins data types 文档并核对本地 Apoli 2.9.2 源码，注册对象型 datatype 节点：`attribute_modifier`、`attributed_attribute_modifier`、`damage_source_description`、`food_component`、`hud_render`、`ingredient`、`item_stack`、`key`、`positioned_item_stack`、`status_effect_instance`、`text`。
- 已修正通用 datatype AST 读写：datatype 节点导出时不写入 Studio 内部节点类型字段；`particle_effect` 后续已改为完整对象导出。
- 已梳理当前注册表中的同名跨 kind 节点，并为 `origins:add_velocity`、`origins:damage`、`origins:execute_command`、`origins:owner`、`origins:resource`、`origins:target_condition` 增加明确标题，避免菜单中误认节点类型。
- 已移除误注册的 `shape-shifter-curse:equip_accessory` entity action；本地 SSC 源码中该 ID 为 entity condition。
- 已将 `origins:and` 在空白处创建菜单中折叠为 `Action And` 与 `Condition And` 两类；内部仍按具体 slotKind 保留上下文 schema，用于端口拖拽时创建正确 action/condition 类型。

## 待办

- 对 `form_axolotl_fountain_belt_trinket.json` 做更接近设计稿的布局调优。
- 建立聚焦的 roundtrip 单元测试。
- 将当前基础导出链路扩展为更完整的 schema 驱动校验与错误定位。
- 为连接校验补齐完整 slotKind/datatype 兼容规则。
- 继续从 Apoli/SSC Java 源补齐低频节点字段，尤其是复杂对象字段、modifier、HUD render、item stack、status effect list 等当前暂以 `unknown` 表示的字段。
- 建立从本地 Java factory 自动抽取候选 schema 的离线生成流程，并对集中注册文件做人工校验，避免误归并字段。

## 已验证

- `npm run build` 已通过。
- 扩展 Apoli/SSC 节点库后，`npm run build` 已通过。
- 注册 Origins/Apoli 对象型 datatype 节点并修正通用 datatype 序列化后，`npm run build` 已通过。
- 调整同名节点标题、移除误注册节点并折叠 `And` 菜单后，`npm run build` 已通过。
- 已用 Node 加载 `electron/workspace.cjs`，确认主进程 workspace 模块语法正常。
- 已确认示例 Power JSON 可以由现有读取 API 提供给前端。
- 已确认目标项目中 39 个 Power 未绑定到任何形态，`form_axolotl_fountain_belt_trinket` 属于该分类。
- 当前尚未在桌面 Electron 窗口中做本轮交互人工验收。
## 2026-06-08 更新

- 已按 Origins data types 截图补齐 Data Type 节点注册，并用本地 Apoli 2.9.2 源码核对存在性。
- 本次注册覆盖 `Action Result`、`Attribute Modifier Operation`、`Attribute Modifier`、`Attributed Attribute Modifier Operation`、`Attributed Attribute Modifier`、`Comparison`、`Container Type`、`Crafting Recipe`、`Damage Source (DEPRECATED)`、`Default Translatable Text Component`、`Destruction Type`、`Entity Type Tag-like`、`Fluid Handling`、`Food Component`、`Heightmap Type`、`Hud Render`、`Identifier`、`Ingredient`、`Inventory Type`、`Item Slot`、`Item Stack`、`Key`、`Material`、`NBT`、`Particle Effect`、`Player Ability`、`Positioned Item Stack`、`Process Mode`、`Shape Type`、`Shape`、`Space`、`Stat`、`Status Effect Instance`、`Text Component`、`Vector`。
- 已新增 `datatype_array` 字段类型，支持 data type 数组参数通过一对多连线构建，并按连线 `order` 导出数组顺序。
- 已补充 primitive/enum 包装型 data type 的解析与导出：节点内用标量字段编辑，保存时回写为 primitive 值或 primitive 数组，避免 `Identifier[]` 等字段被导出成空对象数组。
- 已新增 Apoli 通用 `Modifier` data type 节点，并将 `Modifier.LIST_TYPE` 字段改为一对多 `datatype_array` 端口，修正 `modifiers` 等数组字段显示成不可编辑 `Array` 的问题。
- 已将未知数组字段保留在 `unknownFields`，不再投影成不可编辑参数块。
- 已拆分并标注 `origins:entity_group` 的 Power 与 Entity Condition schema，避免同名节点在创建菜单和解析时混淆。
- 验证：`npm run build` 已通过。

## 2026-06-09 更新

- 已按目标项目现有 `powers/*.json` 实际引用的 `shape-shifter-curse:*` 类型重新扫描 SSC 自定义节点，当前实际 JSON 中出现的 SSC 类型已全部能在 schema 中找到。
- 已修正 `shape-shifter-curse:delay_attribute`：`modifier` 使用 `attributed_attribute_modifier`，`modifiers` 使用 `datatype_array`。
- 已补充 `shape-shifter-curse:mana_modifier` datatype，并修正 `mana_attribute` / `conditioned_mana_attribute` 的 `modifierID`、`max_mana_modifier`、`regen_mana_modifier`、`player_side` 字段。
- 已补齐当前项目中实际出现但之前未注册的 SSC power/action/condition，包括 `bat_block_attach`、`burn_damage_modifier`、`conditioned_modify_slipperiness`、`projectile_dodge`、`set_item_cooldown`、`is_item_in_cooldown` 等。
- 已新增“添加已有 Power 到当前形态”流程：前端提供搜索弹窗，后端通过 `workspace:addPowerToForm` 写入当前形态的 `powers` 引用数组。
- 验证：`npm run build` 已通过；用临时 mock SSC 项目验证 `addPowerToForm` 能成功追加已有 Power；实际目标项目 Power JSON 的 SSC 类型缺失检查结果为 0。
- 已新增“从当前形态移除 Power 注册”流程：前端提供当前形态已注册 Power 的搜索弹窗，后端通过 `workspace:removePowerFromForm` 只移除 origin `powers` 引用，不删除 Power JSON。
- 已优化添加/移除 Power 弹窗列表的长名称显示，Power 名称和类型会按内容换行并自适应行高。
- 已新增 `Raw Json` 特殊 datatype 节点：未知 datatype 参数会投影为 Raw Json 节点，节点内可直接编辑 JSON 文本；保存时 Raw Json 节点内容会还原为对应参数值。
- 验证：`npm run build` 已通过；用临时 mock SSC 项目验证 `removePowerFromForm` 能成功移除已有 Power 注册。

## 2026-06-10 更新

- 已将 `Raw Json` 节点从普通 Data Type 菜单中拆出为独立分类，并使用红色系标注节点、参数和菜单项。
- 已允许 `Raw Json` 作为所有 datatype 端口的兜底创建选项，便于用同一节点验证未知或暂未精确建模的 datatype 参数。
- 已修复对象型 datatype 被错误导出为单值的问题：`ingredient`、`item_stack`、`text`、`crafting_recipe` 等类型现在会保留完整键值对象；只有明确的 primitive/enum wrapper datatype 才允许导出为 primitive 值。
- 验证：`npm run build` 已通过；已对目标项目 450 个 power JSON 执行 `parse -> serialize` 审计，未发现对象字段被导出成非对象。
- 已进一步收紧 datatype 导出规则：除 `Raw Json` 作为显式原始 JSON passthrough 外，所有独立 datatype 节点都按完整对象导出，`particle_effect`、`identifier` 等不再压缩为字符串或 primitive 值。
- 验证：`particle_effect`、`ingredient`、`item_stack`、`identifier` 样例导出均保留键名；目标项目 450 个 power JSON 的对象坍缩审计为 0；`npm run build` 已通过。
